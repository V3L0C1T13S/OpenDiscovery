/* eslint-disable no-redeclare */
export const ServerSchema = {
  $name: String,
  $description: String,
  $icon: String,
};

export interface ServerSchema {
  name: string,
  description: string,
  icon: string,
}
